server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/larblog?serverTimezone=GMT%2B8&characterEncoding=utf-8&useSSL=false
    username: root
    password: baidon@2021
  thymeleaf:
    mode: HTML

mybatis:
  type-aliases-package: main.blog.entity
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

#开启logging myabtis语句打印
logging:
  level:
    main.blog.mapper: trace

pagehelper:
  helperDialect: mysql
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql

larblog:
  upload:
    user-avatar-file-path: "D://larblog/avatar"
