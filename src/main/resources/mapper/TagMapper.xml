<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="main.blog.mapper.TagMapper">

	<!--查询标签-->
	<select id="listTag" parameterType="main.blog.dto.admin.TagSearchDTO" resultType="main.blog.entity.Tag">
		SELECT * FROM fa_tag
    	<where>
	       	 <if test="keywords!= null">
	       	 	tag_name LIKE concat('%',#{keywords},'%')
	       	 </if>
		</where>
    	order by id desc
    </select>

    <!--新增标签-->
    <insert id="addTag" parameterType="main.blog.dto.admin.StatusDTO">
    	insert into fa_tag (tag_name, status, description, create_time) VALUES (#{tagName}, #{status}, #{description}, now())
    </insert>

    <!--获取标签-->
    <select id="infoTag" parameterType="int" resultType="main.blog.entity.Tag">
		SELECT * FROM fa_tag WHERE id = #{id} limit 1
    </select>

    <!--编辑标签-->
    <update id="editTag" parameterType="main.blog.entity.Tag">
    	update fa_tag set tag_name = #{tagName}, status = #{status}, description = #{description}, update_time = now() WHERE id = #{id}
    </update>

    <!--删除标签 -->
    <delete id="deleteTag" parameterType="int">
    	delete FROM fa_tag WHERE id = #{id}
    </delete>

    <!--更新标签状态-->
    <update id="updateTagStatus" parameterType="main.blog.dto.admin.StatusDTO">
    	update fa_tag set status = #{status} WHERE id = #{id}
    </update>

    <!--外部调用-->
	<select id="getListTag" parameterType="int" resultType="main.blog.entity.Tag">
		SELECT * FROM fa_tag ORDER BY id DESC limit #{limit}
    </select>
</mapper>
